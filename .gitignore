# Dependencies
node_modules/

# Build outputs
dist/
build/
coverage/
*.tsbuildinfo

# Runtime data — the store the service writes into (JSON file or SQLite db)
data/
# SQLite sidecar files (journal / write-ahead log)
*.db-journal
*.db-wal
*.db-shm

# Prisma — generated client (when output is set outside node_modules)
prisma/generated/

# Secrets — NEVER commit the real .env, only the .env.example template
.env
.env.*
!.env.example

# Logs
*.log

# Editor / OS noise
.DS_Store
.vscode/
.idea/

# Local scratch
*.local
tmp/
.tmp/
*.tar.gz
*.tar

# Audit / remediation working docs — NEVER commit to a public repo.
# These describe live, unfixed vulnerabilities (responsible disclosure) and
# are internal working notes, not project documentation.
SECURITY-AUDIT-*.md
HANDOFF.md
HANDOFF-*.md
TRIAGE-AND-PLAN.md
TASKS-PR.md
ARCHITECTURE.md
MERGE-GUIDE.md
PR-REVIEW.md
PR3-DESIGN.md
AUDIT-FINDINGS.md

# AI assistant local config / session state
.claude/
CLAUDE.md
AGENTS.md
specs/
docs/plans/
docs/logs/
