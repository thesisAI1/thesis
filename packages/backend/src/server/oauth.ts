/**
 * (Removed) X OAuth 2.0 website login.
 *
 * Author registration no longer happens on the website — payouts are handled
 * entirely on X: the agent replies to a winning thesis asking the author to
 * reply with a Base wallet address (see ../payout). This module is retained
 * only as an empty placeholder and is not imported anywhere.
 */

export {};
